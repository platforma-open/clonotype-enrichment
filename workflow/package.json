{
  "name": "@platforma-open/milaboratories.clonotype-enrichment.workflow",
  "version": "3.1.0",
  "type": "module",
  "description": "Tengo-based template",
  "scripts": {
    "build": "shx rm -rf dist && pl-tengo check && pl-tengo build",
    "format": "/usr/bin/env emacs --script ./format.el",
    "do-pack": "shx rm -f *.tgz && pnpm pack && shx mv *.tgz package.tgz"
  },
  "dependencies": {
    "@platforma-open/milaboratories.clonotype-enrichment.software": "workspace:*",
    "@platforma-sdk/workflow-tengo": "catalog:"
  },
  "devDependencies": {
    "@platforma-open/milaboratories.software-ptransform": "catalog:",
    "@platforma-sdk/tengo-builder": "catalog:"
  }
}
